/*
export function displayImg(bulbImg) {
    bulbImg.map((x) => {
        const img = document.createElement('img')
        img.src = x;
        img.className = "Bulba"; 
        document.body.append(x); 
      })
}
*/ 